# CSharpExcelReader
A lightweight, dependency-free Excel (.xlsx) parser for .NET that reads files as ZIP/XML for extreme performance. Converts large spreadsheets into strongly typed C# entities in seconds. Built to replace heavy libraries like EPPlus when dealing with massive datasets and strict project dependency rules.
